# 增加上传文件功能后的使用指南

## 启动方式

1.在VSCode控制台中输入指令安装解析txt和docx的工具（仅第一次）

```bash
npm install mammoth
```

2.进入后端文件夹输入指令安装解析pdf的工具（仅第一次）

```bash
cd backend
pip install pypdf
```
3.启动脚本（同时启动ai工具）

```bash
python ai_gateway.py
```

4.另起一个控制台启动程序

```bash
npm run dev
```
